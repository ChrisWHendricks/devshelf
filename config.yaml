# code-registry configuration

# Parent directory to scan for git repositories
src_dir: ~/src

# Directories containing Markdown files to browse
md_dirs:
  - ~/src/_scratch-pad
  - ~/Documents

# Server settings
host: 127.0.0.1
port: 8402
